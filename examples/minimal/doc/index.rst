.. Documentation master file

LW Pipeline: Example pipeline steps documentation
=================================================

Steps for the minimal example pipeline

Starts by creating some data in `data/raw` then converts to BIDS format in `data/bids`. 
Finally demonstrate saving of derivatives in `data/derivatives`.

.. toctree::
   :maxdepth: 2
   :caption: Contents:

   api